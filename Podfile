# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'one2one-SwiftUI-Beer-App' do
  pod 'Kingfisher', '~> 7.0'
  use_frameworks!

  # Pods for one2one-SwiftUI-Beer-App

  target 'one2one-SwiftUI-Beer-AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'one2one-SwiftUI-Beer-AppUITests' do
    # Pods for testing
  end

end
